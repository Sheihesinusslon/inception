[www]
user  = nobody
group = nobody

listen = ${PHP_FPM_PORT}

pm                   = dynamic
pm.max_children      = ${PHP_FPM_MAX_CHILDREN}
pm.start_servers     = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3

clear_env = no
